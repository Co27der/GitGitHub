 <--is used to add (stage) changes — meaning, tell Git which files you want to include in the next commit--!>
 
1.  `git add`
2.  `git add file.txt`  Stage a specific file
3. `git add .` tage all changed files in the current folder
4.  `git add -A` Stage all changes (even deletions)
5.  `git add *.py` Stage all .py files in the current directory
