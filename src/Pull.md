[//]: This downloads the latest changes from GitHub and merges them with your local branch
`git pull` Default fetch + merge
`git pull origin <branch>` Pull from a specific branch
`git pull --rebase`Apply local commits on top of remote changes
`git pull --ff-only` Only fast-forward merge, no automatic merge
`git pull --verbose` Show detailed output while pulling