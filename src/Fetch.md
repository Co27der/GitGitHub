<!--It updates information about remote branches without touching your local files.-->
1. `git fetch`
2. `git fetch <remote>`
3.  ` remote`usually ` origin`
4.  `git fetch --all` <!--Fetch updates from all remotes-->
5. `git fetch --prune` <!--Remove references to remote branches that no longer exist.-->
